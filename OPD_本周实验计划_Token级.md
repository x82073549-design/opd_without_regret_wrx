# 本周实验计划 · OPD Token 级特征有效性验证

**范围**:只做 Token 级(最廉、fail-fast)。单一 (student, teacher) 对、单一有 headroom 的 checkpoint。组合(可约散度 / 自信且错)、样本级、teacher 级留到后续。

---

## 1. 四个特征

| 特征                 | 轴 / 角色                |
| ------------------ | --------------------- |
| overlap ratio      | 散度 / 兼容性              |
| teacher confidence | 可靠性                   |
| student entropy    | 学生状态                  |
| FiRe $c_t^T·c_t^S$ | 组合(teacher × student) |

---

## 2. 统一加权(关于 1 中心对称的有界软加权)

对每个特征的逐 token 值 s_t:

1. 序列内标准化:`ẑ_t = (s_t − 序列均值) / 序列标准差`(z-score)。
2. 权重(关于 1 中心对称):
   - 正常:`w_t = 1 + tanh(α · ẑ_t / 2)`(等价于 `2·sigmoid(α · ẑ_t)`)
   - 取反:`w_t = 1 − tanh(α · ẑ_t / 2) = 2 − w_正常`
   - 性质:`ẑ_t = 0`(均值 token)→ `w = 1`(均匀);正常过 1 单调递增、取反过 1 单调递减;`w_正常 + w_取反 = 2`(中心对称);有界于 (0, 2) 不会爆;`α → ∞` 趋于 top/bottom-50% 硬划分。
3. 序列内把扰动项 `tanh(α·ẑ/2)` 先减去其**序列均值**再加 1,使每条序列权重均值恰为 1 —— 保证总梯度不变、不让 token 特征偷偷做样本级加权,同时仍满足 `w_取反 = 2 − w_正常` 的对称。强偏特征 + 大 α 时检查权重非负,必要时下限截断。

四个特征过**同一** harness,三臂:**正常**(+α)/ **取反**(−α)/ **均匀**(α = 0,内含)。

- α 不固定为单值,扫 0.5 / 1 / 2 / 4,报这段强度上的 **AUC**(效应量与 α 耦合)。
- 符号无关:overlap 等方向暧昧的特征不预设方向,+α 与 −α 两臂都跑,从对称曲线读出正确方向。

---

## 3. 因变量与检验

| 阶段 | 因变量 | 种子 | 检验 |
|---|---|---|---|
| 筛选 | held-out reverse-KL | R = 3 | 跨种子组间(Mann–Whitney / t)+ 效应量 |
| 确认(仅胜出 1–2 条件) | accuracy avg@k | R = 5 | 评测配对自助法 / McNemar |

- 期望单调序:`取反 < 均匀 / 随机 < 正常`。
- 可实现增益看 `正常 vs 均匀`;`正常 vs 取反` 仅作下界 sanity。

---
